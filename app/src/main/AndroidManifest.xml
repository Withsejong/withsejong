<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WithSejong"
        tools:targetApi="31">
        <activity
            android:name=".chatting_prototype.Prototype01"
            android:exported="false" />
        <activity
            android:name=".login.signup.SignupPage01"
            android:exported="false" /> <!-- Acticity  정의부 -->
        <activity
            android:name=".login.MainPage"
            android:exported="false" />
        <activity
            android:name=".login.TmpActivity2"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- 첫 화면으로 보여 줄 Acticity -->
        <activity
            android:name=".login.TmpActivity1"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>